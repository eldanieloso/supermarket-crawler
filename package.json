{
  "name": "supermarket-crawler",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d lib --extensions \".ts\" --copy-files",
    "start": "npm run build && cd lib && node index.js",
    "starta": "nodemon --watch src --exec \"npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.16.0",
    "@babel/core": "^7.16.0",
    "@babel/preset-typescript": "^7.16.0",
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "crawler": "^1.3.0"
  }
}
